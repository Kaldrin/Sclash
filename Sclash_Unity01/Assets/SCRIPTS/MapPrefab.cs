using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MapPrefab : MonoBehaviour
{
    [Tooltip("The reference to the parent objects in the map prefab containing the background elements (Everything that is behind the stage) to deactivate for the final death effect")]
    [SerializeField] public GameObject[] backgroundElements = null;









    // BASE FUNCTIONS
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
